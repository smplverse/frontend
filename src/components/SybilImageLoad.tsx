import { useState, useEffect } from 'react'
import { useWaitForTransaction } from 'wagmi'

import { API_URL } from '../constants-sybil'

export const SybilImageLoad = () => {
  // hook to get token id
  // put token id into API_URL
  // fetch minted image

  return (
    <>
      <SybilImageLoad />
    </>
  )
}
